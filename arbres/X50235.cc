#include "BinTree.hh"

bool poda_subarbre(BinTree<int>& a, int x)
// Pre:  un arbre binari d'enters a, sense repeticions, i un enter x
// Post: un valor boolea que indica si 'x' hi es a l'arbre;
//		 s'han eliminat de l'arbre l'elements x i tots els successors
{
	
}
